# Generated by TSPack. Do not edit manually; manifest.tsx is authoritative.
name: VitestNodeUnitCI
on:
  workflow_dispatch: {}
jobs:
  tspack:
    name: TSPack VitestNodeUnitCI
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: "24"
      - name: Setup TSPack
        uses: yuechen-li-dev/tspack/.github/actions/setup-tspack@v0.1.12-m80f.1
        with:
          version: v0.1.12-m80f.1
      - name: Show TSPack version
        run: tspack version
      - name: Verify clean dependency state
        run: test ! -e node_modules && test ! -e .tspack
      - name: Realize locked dependencies
        run: tspack sync --clean
      - name: Check TSPack project
        run: tspack check
      - name: Run TSPack workflow
        run: tspack workflow run VitestNodeUnitCI --ci-provider github
